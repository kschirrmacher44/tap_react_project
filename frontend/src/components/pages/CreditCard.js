
import Button from "../interface/Button";

const CreditCard = (props) => {
  return (
    <div>
      <h2>App store popup placeholder</h2>
      <Button onClick={props.goBack}>Go Back</Button>
    </div>
  );
};

export default CreditCard;
